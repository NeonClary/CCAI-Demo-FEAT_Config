services:
  backend:
    ports:
      - "8000:8000"
    environment:
      CORS_ORIGINS: http://localhost:3000

  frontend:
    ports:
      - "3000:3000"
    environment:
      REACT_APP_API_URL: http://localhost:8000
